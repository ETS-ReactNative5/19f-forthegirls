import React from 'react';
import { StyleSheet, Text, View } from 'react-native';

class Events extends React.Component {
  constructor(props) {
    super(props);
  }

  render() {
    return (
      <Text>I am a event </Text>
    );
  }
}

export default Events;
